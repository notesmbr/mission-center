# Server-side key (NOT exposed to browser) - used by /api/usage to fetch live billing from OpenRouter
OPENROUTER_API_KEY=your_api_key_here

# Legacy / unused
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
